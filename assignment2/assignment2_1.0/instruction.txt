* Compile and run on Linux
1. Open Terminal (Shortcut key on Linux: Ctrl + Alt + T)
2. Jump into assignment folder ( > cd ~/path_to_your_assignment2 )
3. Run the following command:
    $   sh run.sh

Note:
To clean the compiled files: $  make clean
